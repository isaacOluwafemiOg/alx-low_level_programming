#include <stdio.h>
#include "main.h"

/**
 * _strncpy - imitates strcpy
 * @dest: first string input
 * @src: string input to be copied into dest
 * @n: number of bytes of src to be copied
 *
 * Return: resulting string after copying action
 */

char *_strncpy(char *dest, char *src, int n)
{
	char *v;
	v = dest;

	return (v);
}
